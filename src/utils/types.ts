// Enum: Theme
export enum eTheme {
  Primary,
  Secondary,
  Tertiary,
}

// Enum: Size
export enum eSize {
  Tiny,
  XSmall,
  Small,
  Medium,
  Large,
  XLarge,
  XXLarge,
  XXXLarge,
  Big,
  Huge,
}
